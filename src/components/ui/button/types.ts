export enum ButtonSizes {
	sm = "sm",
	md = "md",
	lg = "lg",
}

export enum ButtonVariants {
	outline = "outline",
	filled = "filled",
	link = "link",
}

export enum ButtonColors {
	primary = "primary",
	secondary = "secondary",
}

export enum ButtonTypes {
	button = "button",
	submit = "submit",
}
